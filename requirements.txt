# ── Core runtime (CPU or CUDA) ──────────────────────────────────────────────
# The RunPod PyTorch base image already ships the CUDA build of torch.
# Locally:  pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.4
numpy>=1.26
pyyaml>=6.0
einops>=0.7
tqdm>=4.66

# WOMD Scenario protobuf parsing — no TensorFlow / waymo-open-dataset needed.
blackboxprotobuf>=1.0.1

# Experiment tracking. train.py imports it lazily, so it is only needed when
# `wandb: true` in the config. Run `wandb login` once per pod.
wandb>=0.16

# ── Optional: fused Mamba CUDA kernels ──────────────────────────────────────
# ~10x faster recurrence on GPU; the pure-torch MambaBlock works without them.
# Needs the -devel base image (nvcc) to build.
#   pip install mamba-ssm causal-conv1d
